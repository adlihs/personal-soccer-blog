---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 5);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
		<style>
			main {
				width: 800px;
				max-width: calc(100% - 2em);
				margin: auto;
				padding: 4em 1em;
			}
			.intro {
				margin-bottom: 5rem;
			}
			.intro h1 {
				margin-bottom: 2rem;
			}
			.intro p {
				font-size: 1.15rem;
				line-height: 1.8;
			}
			.latest-analyses {
				margin-top: 4rem;
			}
			.latest-analyses h2 {
				font-size: 2.25rem;
				color: var(--text-primary);
				margin-bottom: 2.5rem;
				display: flex;
				align-items: center;
				gap: 1rem;
			}
			.latest-analyses h2::after {
				content: '';
				flex: 1;
				height: 2px;
				background: linear-gradient(to right, var(--primary), transparent);
			}
			.post-list {
				display: grid;
				grid-template-columns: 1fr;
				gap: 2rem;
				list-style-type: none;
				padding: 0;
			}
			.post-item {
				transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.3s ease;
			}
			.post-item:hover {
				transform: scale(1.02) translateY(-4px);
				box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
			}
			.post-item a {
				display: grid;
				grid-template-columns: 320px 1fr;
				text-decoration: none;
				color: inherit;
				overflow: hidden;
				border-radius: 16px;
			}
			.post-item img {
				width: 100%;
				height: 220px;
				object-fit: cover;
				transition: transform 0.5s ease;
			}
			.post-item:hover img {
				transform: scale(1.05);
			}
			.post-content {
				padding: 2rem;
				display: flex;
				flex-direction: column;
				justify-content: center;
			}
			.post-content h4 {
				margin-bottom: 0.75rem;
				font-size: 1.5rem;
				line-height: 1.3;
				transition: color 0.2s ease;
			}
			.post-item:hover .post-content h4 {
				color: var(--primary);
			}
			.post-date {
				font-size: 0.85rem;
				color: var(--text-secondary);
				display: flex;
				align-items: center;
				gap: 0.5rem;
			}
			.post-date::before {
				content: '●';
				color: var(--primary);
				font-size: 0.5rem;
			}
			@media (max-width: 800px) {
				.post-item a {
					grid-template-columns: 1fr;
				}
				.post-item img {
					height: 200px;
				}
				.post-content {
					padding: 1.5rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="intro animate-fade-up">
				<h1>⚽ {SITE_TITLE}</h1>
				<p>
					Bienvenido a tu plataforma de referencia para el <strong>análisis táctico y estadístico</strong> del fútbol moderno. 
					En {SITE_TITLE}, nos apasiona descifrar los patrones ocultos detrás de cada partido, equipo y jugador.
				</p>
				<p>
					Nuestra misión es transformar la complejidad del juego en <em>insights</em> accionables, utilizando datos 
					avanzados y una mirada táctica profunda para entender no solo qué pasó en el marcador, sino <strong>cómo y por qué</strong> sucedió en el césped.
				</p>
				<p>
					Únete a la conversación mientras exploramos desde las pizarras de los entrenadores más innovadores 
					hasta las métricas que están revolucionando el scouting mundial.
				</p>
			</section>

			<section class="latest-analyses animate-fade-up" style="animation-delay: 0.2s;">
				<h2 class="technical">Últimos Análisis</h2>
				<ul class="post-list">
					{
						posts.map((post) => (
							<li class="post-item glass">
								<a href={`/blog/${post.id}/`}>
									<img src={post.data.heroImage} alt="" />
									<div class="post-content">
										<h4>{post.data.title}</h4>
										<span class="post-date technical">
											<FormattedDate date={post.data.pubDate} />
										</span>
									</div>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
